[pytest]
filterwarnings =
    # Ignore deprecation warnings from libraries we don't control
    ignore::DeprecationWarning
    # Specifically ignore the Google GenAI typing issue
    ignore:.*_UnionGenericAlias.*:DeprecationWarning